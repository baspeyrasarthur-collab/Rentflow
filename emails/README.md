# RentFlow emails

Email templates will live here.

The MVP starts with a mock EmailProvider so development never sends real
transactional emails by accident. Resend can be wired behind the same interface
later.
